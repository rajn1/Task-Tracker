<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaskTracker_V1.Views.ItemsPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:TaskTracker_V1.ViewModels"  
             xmlns:model="clr-namespace:TaskTracker_V1.Models"  
             x:Name="BrowseItemsPage">
    <!-- App New Task Button-->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="New Task" Command="{Binding AddItemCommand}" />
    </ContentPage.ToolbarItems>
    <!--
      x:DataType enables compiled bindings for better performance and compile time validation of binding expressions.
      https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/data-binding/compiled-bindings
    -->
    <StackLayout>
        <StackLayout Orientation="Horizontal">
            <Button Text="Add" Clicked="OnAdd" />
            <Button Text="Update" Clicked="OnUpdate" HorizontalOptions="CenterAndExpand" />
            <Button Text="Delete" Clicked="OnDelete" />
        </StackLayout>
        <ListView x:Name="timeEntriesListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
